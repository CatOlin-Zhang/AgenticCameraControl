# Core dependencies for xpai-camera-control
# Install: pip install -r requirements.txt

# MCP Server
mcp>=1.0.0, <2.0.0

# Camera control
opencv-python>=4.8.0
onvif-zeep>=0.2.12
requests>=2.28.0
psutil>=5.9.0

# ffmpeg binaries (for toggle_recording / ffprobe stream detection)
# imageio-ffmpeg bundles cross-platform ffmpeg & ffprobe binaries,
# auto-downloaded on first use. No separate system install required.
imageio-ffmpeg>=0.5.1

# Configuration
PyYAML>=6.0
